apiVersion: v1
kind: Service
metadata:
  name: demo-ui
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector: { app: demo-ui }
  ports:
    - { name: http, port: {{ .Values.service.port }}, targetPort: http }
