namespace: platform
image:
  repository: nginx
  tag: "1.27-alpine"
service:
  port: 80
  # ClusterIP; accessed via port-forward (see scripts/port-forward-ui.sh).
gatewayUrl: "http://localhost:8080"  # rewritten at runtime via window.location
dexUrl: "http://localhost:5556"
