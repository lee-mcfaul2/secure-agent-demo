apiVersion: v1
kind: Service
metadata:
  name: agent-sql-mcp
  namespace: {{ .Values.namespace }}
spec:
  selector: { app: agent-sql-mcp }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8443
