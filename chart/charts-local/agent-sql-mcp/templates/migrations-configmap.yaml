apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-sql-mcp-migrations
  namespace: {{ .Values.namespace }}
data:
{{- range $path, $bytes := .Files.Glob "migrations/*.sql" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
