apiVersion: v1
kind: Service
metadata:
  name: agent-gateway
  namespace: {{ .Values.namespace }}
spec:
  selector: { app: agent-gateway }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
