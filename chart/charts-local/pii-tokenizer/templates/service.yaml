apiVersion: v1
kind: Service
metadata:
  name: pii-tokenizer
  namespace: {{ .Values.namespace }}
spec:
  selector: { app: pii-tokenizer }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8443
