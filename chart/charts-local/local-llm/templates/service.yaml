apiVersion: v1
kind: Service
metadata:
  name: local-llm
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector: { app: local-llm }
  ports:
    - { name: http, port: {{ .Values.service.port }}, targetPort: http }
