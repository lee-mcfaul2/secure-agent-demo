apiVersion: v1
kind: ConfigMap
metadata:
  name: traffic-gen-script
  namespace: {{ .Values.namespace }}
data:
  traffic_gen.py: |
    # Populated in T18 — this placeholder lets the chart render until then.
    print("traffic-gen placeholder; replaced in T18")
