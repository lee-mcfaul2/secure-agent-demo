apiVersion: v1
kind: ConfigMap
metadata:
  name: llm-guard-pii-types
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "llm-guard.labels" . | nindent 4 }}
data:
  pii-types.json: |
{{ .Values.piiTypes.content | indent 4 }}
