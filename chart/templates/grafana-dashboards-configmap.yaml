apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-security-dashboards
  namespace: platform
  labels:
    grafana_dashboard: "1"
data:
  platform-overview.json: |
{{ .Files.Get "dashboards/platform-overview.json" | indent 4 }}
  security-posture.json: |
{{ .Files.Get "dashboards/security-posture.json" | indent 4 }}
  agent-loop.json: |
{{ .Files.Get "dashboards/agent-loop.json" | indent 4 }}
